import React from "react";

const TodoList = ({ items }) => {
  return (
    <main>
      {" "}
      <ul className="todo-list">
        <h3>todo list</h3>
      </ul>
    </main>
  );
};

export default TodoList;
